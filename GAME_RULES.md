# ミニゲーム仕様・動作メモ

このファイルは、将来の機能拡張や修正に使う開発者向け資料です。プレイヤーには表示しません。

## 共通仕様

- 各ゲームは独立したディレクトリ内の単一HTMLファイルで動作する。
- フレームワーク、ビルドツール、外部パッケージは使用しない。
- 問題やカードのデータは、Google Sheets API v4で同じスプレッドシートから取得する。
- 各ゲームは `../config.js` を読み込む。
- データ取得範囲は原則として2行目以降を使用する。1行目は見出し行。
- 画面は `.screen` と `active` クラスの切り替えで管理する。
- `config.js` の `MAX_QUESTIONS` があるゲームは、取得データをシャッフルして最大問題数に絞る。
- 結果画面では `ACHIEVEMENT_MIN_SCORE` と正答率を比較する。達成時はブラウザで現在日時を生成し、景品提示用の画面に表示する。
- トップページは設定シートの `ACTIVE_GAME` を優先してゲームへ転送する。取得失敗時は `config.js` の値へフォールバックする。
- `ACTIVE_GAME` は `quiz`、`crossword`、`visual-quiz`、`word-order`、`memory-game` のいずれか。末尾の `/` は任意。

## 1. 4択クイズ

### ファイル

`quiz/index.html`

### ルール

- 問題文と4つの選択肢を表示する。
- 選択肢を1つタップすると、その問題の回答を確定する。
- 正解なら正解数を1加算する。
- 回答後は正解・不正解と解説を表示し、次の問題へ進む。
- 全問題終了後に正解数と正答率を表示する。
- 正答率が `ACHIEVEMENT_MIN_SCORE` 以上なら景品提示画面を表示する。

### データ

`SHEET_NAME` のシートから `A2:H1000` を取得する。

| 列 | 内容 |
|---|---|
| A | 問題番号 |
| B | 問題文 |
| C | 正解 |
| D〜F | 不正解3つ |
| G | 解説 |
| H | カテゴリ |

C列が正解として扱われ、C〜F列の4値を表示時にシャッフルする。

## 2. クロスワードパズル

### ファイル

`crossword/index.html`

### ルール

- 1行の横単語と縦単語を1問として扱う。
- 横単語と縦単語で最初に一致した文字を交差位置にする。
- 交差位置だけが入力対象で、周囲の文字は固定表示する。
- 入力対象をタップすると、カナ行を選ぶダイアログを開く。
- カ行・サ行・タ行・ハ行は通常音、濁音、半濁音を同じ画面に表示する。
- `答え合わせ` で各交差文字を採点し、結果画面へ進む。
- 入力途中でも答え合わせ可能。正解数と正答率で景品達成を判定する。
- 横単語と縦単語に共通文字がない行は自動的に除外する。

### データ

`CROSSWORD_SHEET` のシートから `A2:C1000` を取得する。

| 列 | 内容 |
|---|---|
| A | 横単語（カタカナ） |
| B | 縦単語（カタカナ） |
| C | ヒント。横・縦の順に `/` で区切る |

### 注意点

- 最初に見つかった共通文字だけを交差位置にする。
- 単語の文字数や画面幅によってマスサイズを計算する。
- 同じ単語内に複数の共通文字があっても、複数交差にはならない。

## 3. 写真・シルエットクイズ

### ファイル

`visual-quiz/index.html`

### ルール

- 写真またはシルエットを1問ずつ表示する。
- 画像を見て、正式名称を4択から選ぶ。
- 回答後に正解・不正解と解説を表示し、一定時間後に次の問題へ進む。
- I列が `シルエット` の場合、画像にCSSの `brightness(0)` フィルターを適用する。
- I列が `写真` または空欄の場合は通常画像として表示する。
- Google Driveの共有URLはサムネイルURLへ変換して表示する。
- 結果画面の景品達成判定は通常の4択クイズと同じ。

### データ

`VISUAL_SHEET` のシートから `A2:I1000` を取得する。

| 列 | 内容 |
|---|---|
| A | 問題番号 |
| B | 問題文 |
| C | 画像URL |
| D | 正解 |
| E〜G | 不正解3つ |
| H | 解説 |
| I | 出題形式。`写真` または `シルエット` |

### 注意点

- Google Drive画像は、閲覧者がアクセスできる共有設定にする必要がある。
- 画像が読み込めない場合でも、現在は画像エラー専用表示を行わずブラウザの画像表示状態に依存する。
- シルエットは元画像の輪郭を黒くする方式で、専用のシルエット画像を生成するものではない。

## 4. 文字並べ替えクイズ

### ファイル

`word-order/index.html`

### ルール

- 問題文を読み、文字カードを順番にタップして回答を作る。
- 正解文字列を1文字ずつ分解し、カードとしてシャッフル表示する。
- 選択したカードの文字を、回答欄の左から順番に入れる。
- すべての回答欄が埋まった時点で自動採点する。
- 使用済みカードは薄く表示し、同じカードを再選択できないようにする。
- `1文字もどす` は直前に選んだカードだけを未使用に戻す。
- `全部消す` は回答欄とカードの使用状態を初期化する。
- 正解時だけ解説を表示する。不正解時は正解文字列や解説を表示しない。
- 全問題終了後、正答率に応じて景品提示画面を表示する。

### データ

`WORD_ORDER_SHEET` のシートから `A2:D1000` を取得する。

| 列 | 内容 |
|---|---|
| A | 問題文 |
| B | 予備項目。現在は表示・判定に使用しない |
| C | 正解文字列。例: `マツタケ` |
| D | 解説 |

### 注意点

- 正解判定はカードを選んだ順番とC列の文字列が完全一致するかで行う。
- 同じ文字が複数ある答えでは、カードを内部インデックスで区別する。
- 現在はひらがな・カタカナの変換や表記ゆれを吸収しない。問題データと入力カードの表記を統一する。
- ドラッグ操作は実装せず、タップ操作を基本とする。

## 5. 絵合わせ（神経衰弱）

### ファイル

`memory-game/index.html`

### ルール

- 各カードデータを2枚ずつ複製し、全カードをシャッフルする。
- プレイヤーがカードを2枚タップすると、同じIDか判定する。
- 同じIDならマッチ済みとして残す。
- 異なるIDなら少し待ってから裏返す。
- すべてのペアが揃うと結果画面を表示する。
- 結果画面では、2枚めくる操作を1回とした総めくり回数を表示する。
- 現在は景品提示画面を使用しない。

### データ

`MEMORY_SHEET` のシートから `A2:C1000` を取得する。

| 列 | 内容 |
|---|---|
| A | カードのラベル |
| B | 画像URL。空欄なら絵文字を使用 |
| C | 画像がない場合の絵文字 |

### 注意点

- 同じカードの判定には表示ラベルではなく、生成したIDを使用する。
- Google Driveの画像URLはサムネイルURLへ変換する。
- データが2件未満の場合はゲームを開始できない。
- カード数を増やすと4列グリッドの縦長画面になる。

## 拡張時の確認項目

- 新しいゲームのフォルダと `index.html` を追加する。
- `config.js` にシート名を追加する。
- ルート `index.html` のリンクと `allowedGames` に追加する。
- 設定シートで選択可能にする場合、トップページの許可リストへ追加する。
- このファイルにルール、データ列、判定ロジック、注意点を追記する。
- 高齢者向けの大きな文字・タップ領域・明確な正誤表示を維持する。
